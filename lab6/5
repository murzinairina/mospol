#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	setlocale(0, "");
	int x, y;
	cout << "Введите значение X: " << "\n";
	cin >> x;
	y = 4 * pow(x - 3, 6) - 7 * pow(x - 3, 3) + 2;
	cout << "Y = ";
	cout << y;
}
